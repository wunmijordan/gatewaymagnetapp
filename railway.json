{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
  },
  "deploy": {
    "startCommand": "gunicorn gatewaymagnetapp.wsgi:application --bind 0.0.0.0:8000 --workers=4 --threads=2 --timeout=120"
  }
}
